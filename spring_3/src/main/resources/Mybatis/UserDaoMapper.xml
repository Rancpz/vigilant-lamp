<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="Mybatis.Dao.UserDao">

    <select id="findAllUser" resultType="Mybatis.entity.User">
        SELECT * from user
    </select>

    <!--    <select id="insertUser" parameterType="Mybatis.entity.User">-->
    <!--        INSERT INTO user VALUE (#{id}, #{name}, #{age}, #{bir} )-->
    <!--    </select>-->
</mapper>
